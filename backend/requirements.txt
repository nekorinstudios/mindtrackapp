# MindTrack backend runtime dependencies.
# Only packages actually imported by server.py or required transitively.
# Rebuild the exhaustive freeze with: `pip install -r requirements.txt && pip freeze > requirements.txt`

# --- FastAPI + ASGI stack ---
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
click==8.3.2
h11==0.16.0
httpx==0.28.1
httpcore==1.0.9
anyio==4.13.0
sniffio==1.3.1

# --- Pydantic (models + email validation) ---
pydantic==2.12.5
pydantic_core==2.41.5
annotated-types==0.7.0
typing-inspection==0.4.2
typing_extensions==4.15.0
email-validator==2.3.0

# --- Auth ---
bcrypt==4.1.3
PyJWT==2.12.1

# --- MongoDB (async) ---
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0

# --- Payments ---
stripe==15.0.1

# --- Email ---
resend==2.29.0

# --- HTTP client used transitively by stripe + resend ---
requests==2.33.1
urllib3==2.6.3
charset-normalizer==3.4.7
certifi==2026.2.25
idna==3.11

# --- Config / uploads ---
python-dotenv==1.2.2
python-multipart==0.0.24
